import sys
import os

# Add the directory containing your Python script to the system path
sys.path.insert(0, '/var/www/html/rental')

# Import your Python script
from RenApp import app as application